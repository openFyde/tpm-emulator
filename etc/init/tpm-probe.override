start on started boot-services and started tpm-emulator

pre-start script
  logger -t "$UPSTART_JOB" "tpm-probe starting."
end script

post-start script
  logger -t "$UPSTART_JOB" "tpm-probe started."
end script
